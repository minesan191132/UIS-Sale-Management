# Database Configuration
server.port=${PORT:8080}

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:UIS_SaleSystem}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Enable DevTools
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# Additional DevTools configuration
spring.devtools.restart.poll-interval=2s
spring.devtools.restart.quiet-period=1s
spring.devtools.livereload.port=35729

spring.thymeleaf.cache=false

# Include templates in restart triggers
spring.devtools.restart.additional-paths=src/main/resources/templates

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=true
spring.flyway.repair-on-migrate=true

# JWT Authentication Configuration
jwt.secret=YourSuperSecretKeyForJWTTokenGenerationMinimum512BitsLongDoNotCommitToGitChangeInProduction
jwt.expiration=86400000

# Frontend/Backend URL + CORS (override on Railway via env vars)
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}
app.backend.url=${APP_BACKEND_URL:http://localhost:8080}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,https://uis-sale-management.vercel.app}
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:https://*.vercel.app}

# =============================================
# SePay Configuration
# =============================================
# So tai khoan ngan hang cua ban
sepay.bank.account=0934103182
# Ma ngan hang SePay (MB = MBBank, VCB = Vietcombank, TCB = Techcombank)
# Xem danh sach day du tai: https://api.sepay.vn/v1/banks
# TODO: Chinh lai thanh dung ngan hang cua ban!
sepay.bank.code=MBBank
# Webhook token tu dashboard SePay (dang nhap https://my.sepay.vn)
# De trong neu chua dang ky SePay (QR van hoat dong, chi khong co webhook)
sepay.webhook.token=Vietngu

# Logging
logging.level.org.example=DEBUG
logging.level.org.springframework.security=DEBUG

spring.data.redis.host=${REDIS_HOST:${REDISHOST:localhost}}
spring.data.redis.port=${REDIS_PORT:${REDISPORT:6379}}
spring.data.redis.password=${REDIS_PASSWORD:${REDISPASSWORD:}}
spring.data.redis.username=${REDIS_USERNAME:${REDISUSER:}}
spring.data.redis.ssl.enabled=${REDIS_SSL_ENABLED:${REDIS_TLS_ENABLED:false}}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=longndts01333@fpt.edu.vn
spring.mail.password=wsbt shuz isna tayd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =============================================
# Contact Form Configuration
# =============================================
# ✅ Chỉnh email này để thay đổi địa chỉ nhận yêu cầu liên hệ
contact.receiver.email=hoanganh070607@gmail.com